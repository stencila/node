language: node_js

node_js:
  - 8

addons:
  apt:
    packages:
      - python
      - python-pip

before_install:
  - pip install --user travis ipykernel

cache:
  directories:
    - node_modules

script:
  - make lint
  - make check
  - make cover
  - make docs

after_success:
  - bash <(curl -s https://codecov.io/bash)
